From 1b2fa6d6881b46aaabea68e51ef872f5346522ba Mon Sep 17 00:00:00 2001
From: Yijie Yang <quic_yijiyang@quicinc.com>
Date: Wed, 20 Nov 2024 16:54:17 +0800
Subject: [PATCH 68/68] fix speed switch error

Signed-off-by: Yijie Yang <quic_yijiyang@quicinc.com>
---
 arch/arm64/boot/dts/qcom/qcs615-ride.dts      |  1 -
 arch/arm64/configs/defconfig                  |  1 +
 drivers/clk/clk.c                             |  2 +-
 .../stmicro/stmmac/dwmac-qcom-ethqos.c        | 74 +++++++++----------
 .../net/ethernet/stmicro/stmmac/stmmac_main.c |  1 +
 drivers/net/mdio.c                            |  1 +
 drivers/net/mii.c                             |  1 +
 drivers/net/phy/micrel.c                      |  2 +
 drivers/net/phy/phy-c45.c                     |  1 +
 drivers/net/phy/phy.c                         | 11 ++-
 drivers/net/phy/phy_device.c                  |  5 +-
 drivers/net/phy/phylink.c                     |  2 +
 drivers/net/phy/qcom/qcom-phy-lib.c           |  1 +
 13 files changed, 57 insertions(+), 46 deletions(-)

diff --git a/arch/arm64/boot/dts/qcom/qcs615-ride.dts b/arch/arm64/boot/dts/qcom/qcs615-ride.dts
index a272686efbc2..5ccacc62d28c 100644
--- a/arch/arm64/boot/dts/qcom/qcs615-ride.dts
+++ b/arch/arm64/boot/dts/qcom/qcs615-ride.dts
@@ -286,7 +286,6 @@ &ethernet {
 
 	phy-handle = <&rgmii_phy>;
 	phy-mode = "rgmii";
-	max-speed = <1000>;
 
 	snps,mtl-rx-config = <&mtl_rx_setup>;
 	snps,mtl-tx-config = <&mtl_tx_setup>;
diff --git a/arch/arm64/configs/defconfig b/arch/arm64/configs/defconfig
index 9b38a977aa0d..7f9bcff618a7 100644
--- a/arch/arm64/configs/defconfig
+++ b/arch/arm64/configs/defconfig
@@ -1741,3 +1741,4 @@ CONFIG_CORESIGHT_STM=m
 CONFIG_CORESIGHT_CPU_DEBUG=m
 CONFIG_CORESIGHT_CTI=m
 CONFIG_MEMTEST=y
+CONFIG_DYNAMIC_DEBUG=y
diff --git a/drivers/clk/clk.c b/drivers/clk/clk.c
index bdc6e5b90da5..50a8549573f0 100644
--- a/drivers/clk/clk.c
+++ b/drivers/clk/clk.c
@@ -3436,7 +3436,7 @@ static int clk_dump_show(struct seq_file *s, void *data)
 }
 DEFINE_SHOW_ATTRIBUTE(clk_dump);
 
-#undef CLOCK_ALLOW_WRITE_DEBUGFS
+#define CLOCK_ALLOW_WRITE_DEBUGFS
 #ifdef CLOCK_ALLOW_WRITE_DEBUGFS
 /*
  * This can be dangerous, therefore don't provide any real compile time
diff --git a/drivers/net/ethernet/stmicro/stmmac/dwmac-qcom-ethqos.c b/drivers/net/ethernet/stmicro/stmmac/dwmac-qcom-ethqos.c
index 8c76beaee488..7123cad2eb71 100644
--- a/drivers/net/ethernet/stmicro/stmmac/dwmac-qcom-ethqos.c
+++ b/drivers/net/ethernet/stmicro/stmmac/dwmac-qcom-ethqos.c
@@ -148,25 +148,23 @@ static void rgmii_dump(void *priv)
 	struct qcom_ethqos *ethqos = priv;
 	struct device *dev = &ethqos->pdev->dev;
 
-	dev_dbg(dev, "Rgmii register dump\n");
-	dev_dbg(dev, "RGMII_IO_MACRO_CONFIG: %x\n",
-		rgmii_readl(ethqos, RGMII_IO_MACRO_CONFIG));
-	dev_dbg(dev, "SDCC_HC_REG_DLL_CONFIG: %x\n",
-		rgmii_readl(ethqos, SDCC_HC_REG_DLL_CONFIG));
-	dev_dbg(dev, "SDCC_HC_REG_DDR_CONFIG: %x\n",
-		rgmii_readl(ethqos, SDCC_HC_REG_DDR_CONFIG));
-	dev_dbg(dev, "SDCC_HC_REG_DLL_CONFIG2: %x\n",
-		rgmii_readl(ethqos, SDCC_HC_REG_DLL_CONFIG2));
-	dev_dbg(dev, "SDC4_STATUS: %x\n",
-		rgmii_readl(ethqos, SDC4_STATUS));
-	dev_dbg(dev, "SDCC_USR_CTL: %x\n",
-		rgmii_readl(ethqos, SDCC_USR_CTL));
-	dev_dbg(dev, "RGMII_IO_MACRO_CONFIG2: %x\n",
-		rgmii_readl(ethqos, RGMII_IO_MACRO_CONFIG2));
-	dev_dbg(dev, "RGMII_IO_MACRO_DEBUG1: %x\n",
-		rgmii_readl(ethqos, RGMII_IO_MACRO_DEBUG1));
-	dev_dbg(dev, "EMAC_SYSTEM_LOW_POWER_DEBUG: %x\n",
-		rgmii_readl(ethqos, EMAC_SYSTEM_LOW_POWER_DEBUG));
+	dev_info(dev, "Rgmii register dump\n");
+	dev_info(dev, "RGMII_IO_MACRO_CONFIG: %x\n",
+		 rgmii_readl(ethqos, RGMII_IO_MACRO_CONFIG));
+	dev_info(dev, "SDCC_HC_REG_DLL_CONFIG: %x\n",
+		 rgmii_readl(ethqos, SDCC_HC_REG_DLL_CONFIG));
+	dev_info(dev, "SDCC_HC_REG_DDR_CONFIG: %x\n",
+		 rgmii_readl(ethqos, SDCC_HC_REG_DDR_CONFIG));
+	dev_info(dev, "SDCC_HC_REG_DLL_CONFIG2: %x\n",
+		 rgmii_readl(ethqos, SDCC_HC_REG_DLL_CONFIG2));
+	dev_info(dev, "SDC4_STATUS: %x\n", rgmii_readl(ethqos, SDC4_STATUS));
+	dev_info(dev, "SDCC_USR_CTL: %x\n", rgmii_readl(ethqos, SDCC_USR_CTL));
+	dev_info(dev, "RGMII_IO_MACRO_CONFIG2: %x\n",
+		 rgmii_readl(ethqos, RGMII_IO_MACRO_CONFIG2));
+	dev_info(dev, "RGMII_IO_MACRO_DEBUG1: %x\n",
+		 rgmii_readl(ethqos, RGMII_IO_MACRO_DEBUG1));
+	dev_info(dev, "EMAC_SYSTEM_LOW_POWER_DEBUG: %x\n",
+		 rgmii_readl(ethqos, EMAC_SYSTEM_LOW_POWER_DEBUG));
 }
 
 /* Clock rates */
@@ -250,12 +248,12 @@ static const struct ethqos_emac_driver_data emac_v2_1_0_data = {
 };
 
 static const struct ethqos_emac_por emac_v2_3_1_por[] = {
-	{ .offset = RGMII_IO_MACRO_CONFIG,	.value = 0x00C01343 },
-	{ .offset = SDCC_HC_REG_DLL_CONFIG,	.value = 0x2004642C },
-	{ .offset = SDCC_HC_REG_DDR_CONFIG,	.value = 0x00000000 },
-	{ .offset = SDCC_HC_REG_DLL_CONFIG2,	.value = 0x00200000 },
-	{ .offset = SDCC_USR_CTL,		.value = 0x00010800 },
-	{ .offset = RGMII_IO_MACRO_CONFIG2,	.value = 0x00002060 },
+	{ .offset = RGMII_IO_MACRO_CONFIG, .value = 0x00C01343 },
+	{ .offset = SDCC_HC_REG_DLL_CONFIG, .value = 0x2004642C },
+	{ .offset = SDCC_HC_REG_DDR_CONFIG, .value = 0x00000000 },
+	{ .offset = SDCC_HC_REG_DLL_CONFIG2, .value = 0x00200000 },
+	{ .offset = SDCC_USR_CTL, .value = 0x00010800 },
+	{ .offset = RGMII_IO_MACRO_CONFIG2, .value = 0x00002060 },
 };
 
 static const struct ethqos_emac_driver_data emac_v2_3_1_data = {
@@ -501,13 +499,9 @@ static int ethqos_rgmii_macro_init(struct qcom_ethqos *ethqos)
 		rgmii_updatel(ethqos, RGMII_CONFIG2_RSVD_CONFIG15,
 			      0, RGMII_IO_MACRO_CONFIG2);
 
-		if (ethqos->has_emac_ge_3)
-			rgmii_updatel(ethqos, RGMII_CONFIG2_RX_PROG_SWAP,
-				      RGMII_CONFIG2_RX_PROG_SWAP,
-				      RGMII_IO_MACRO_CONFIG2);
-		else
-			rgmii_updatel(ethqos, RGMII_CONFIG2_RX_PROG_SWAP,
-				      0, RGMII_IO_MACRO_CONFIG2);
+		rgmii_updatel(ethqos, RGMII_CONFIG2_RX_PROG_SWAP,
+			      RGMII_CONFIG2_RX_PROG_SWAP,
+			      RGMII_IO_MACRO_CONFIG2);
 
 		/* Write 0x5 to PRG_RCLK_DLY_CODE */
 		rgmii_updatel(ethqos, SDCC_DDR_CONFIG_EXT_PRG_RCLK_DLY_CODE,
@@ -541,13 +535,10 @@ static int ethqos_rgmii_macro_init(struct qcom_ethqos *ethqos)
 			      RGMII_IO_MACRO_CONFIG);
 		rgmii_updatel(ethqos, RGMII_CONFIG2_RSVD_CONFIG15,
 			      0, RGMII_IO_MACRO_CONFIG2);
-		if (ethqos->has_emac_ge_3)
-			rgmii_updatel(ethqos, RGMII_CONFIG2_RX_PROG_SWAP,
-				      RGMII_CONFIG2_RX_PROG_SWAP,
-				      RGMII_IO_MACRO_CONFIG2);
-		else
-			rgmii_updatel(ethqos, RGMII_CONFIG2_RX_PROG_SWAP,
-				      0, RGMII_IO_MACRO_CONFIG2);
+
+		rgmii_updatel(ethqos, RGMII_CONFIG2_RX_PROG_SWAP,
+			      RGMII_CONFIG2_RX_PROG_SWAP,
+			      RGMII_IO_MACRO_CONFIG2);
 		/* Write 0x5 to PRG_RCLK_DLY_CODE */
 		rgmii_updatel(ethqos, SDCC_DDR_CONFIG_EXT_PRG_RCLK_DLY_CODE,
 			      (BIT(29) | BIT(27)), SDCC_HC_REG_DDR_CONFIG);
@@ -635,7 +626,8 @@ static int ethqos_configure_rgmii(struct qcom_ethqos *ethqos)
 		if (!retry)
 			dev_err(dev, "Timeout while waiting for DLL lock\n");
 	}
-
+	pr_info("yyj debug: %s:%d ethqos->speed=%u", __func__, __LINE__,
+		ethqos->speed);
 	if (ethqos->speed == SPEED_1000)
 		ethqos_dll_configure(ethqos);
 
@@ -824,6 +816,8 @@ static int qcom_ethqos_probe(struct platform_device *pdev)
 		return -ENOMEM;
 
 	ret = of_get_phy_mode(np, &ethqos->phy_mode);
+	pr_info("yyj debug: %s:%d ethqos->phy_mode=%d", __func__, __LINE__,
+		ethqos->phy_mode);
 	if (ret)
 		return dev_err_probe(dev, ret, "Failed to get phy mode\n");
 	switch (ethqos->phy_mode) {
diff --git a/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c b/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
index 3cdc3910f3a0..028b09ff52e7 100644
--- a/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
+++ b/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
@@ -4483,6 +4483,7 @@ static netdev_tx_t stmmac_xmit(struct sk_buff *skb, struct net_device *dev)
 	int entry, first_tx;
 	dma_addr_t des;
 
+	pr_info("yyj debug: %s:%d", __func__, __LINE__);
 	tx_q = &priv->dma_conf.tx_queue[queue];
 	txq_stats = &priv->xstats.txq_stats[queue];
 	first_tx = tx_q->cur_tx;
diff --git a/drivers/net/mdio.c b/drivers/net/mdio.c
index f67a4d4005e7..1759d9f83649 100644
--- a/drivers/net/mdio.c
+++ b/drivers/net/mdio.c
@@ -300,6 +300,7 @@ void mdio45_ethtool_ksettings_get_npage(const struct mdio_if_info *mdio,
 			cmd->base.duplex = !(modes & ADVERTISED_1000baseT_Half);
 		} else if (modes & (ADVERTISED_100baseT_Full |
 				    ADVERTISED_100baseT_Half)) {
+			pr_info("yyj debug: %s:%d", __func__, __LINE__);
 			speed = SPEED_100;
 			cmd->base.duplex = !!(modes & ADVERTISED_100baseT_Full);
 		} else {
diff --git a/drivers/net/mii.c b/drivers/net/mii.c
index 22680f47385d..b34a61de94f5 100644
--- a/drivers/net/mii.c
+++ b/drivers/net/mii.c
@@ -193,6 +193,7 @@ void mii_ethtool_get_link_ksettings(struct mii_if_info *mii,
 			cmd->base.duplex = !!(nego & ADVERTISED_1000baseT_Full);
 		} else if (nego & (ADVERTISED_100baseT_Full |
 				   ADVERTISED_100baseT_Half)) {
+			pr_info("yyj debug: %s:%d", __func__, __LINE__);
 			cmd->base.speed = SPEED_100;
 			cmd->base.duplex = !!(nego & ADVERTISED_100baseT_Full);
 		} else {
diff --git a/drivers/net/phy/micrel.c b/drivers/net/phy/micrel.c
index 3ef508840674..58028eed309d 100644
--- a/drivers/net/phy/micrel.c
+++ b/drivers/net/phy/micrel.c
@@ -734,6 +734,7 @@ static int ksz8041_config_aneg(struct phy_device *phydev)
 	/* Skip auto-negotiation in fiber mode */
 	if (phydev->dev_flags & MICREL_PHY_FXEN) {
 		phydev->speed = SPEED_100;
+		pr_info("yyj debug: %s:%d", __func__, __LINE__);
 		return 0;
 	}
 
@@ -1556,6 +1557,7 @@ static int ksz8873mll_read_status(struct phy_device *phydev)
 	else
 		phydev->speed = SPEED_100;
 
+	pr_info("yyj debug: %s:%d", __func__, __LINE__);
 	phydev->link = 1;
 	phydev->pause = phydev->asym_pause = 0;
 
diff --git a/drivers/net/phy/phy-c45.c b/drivers/net/phy/phy-c45.c
index 96d0b3a5a9d3..57249b391d64 100644
--- a/drivers/net/phy/phy-c45.c
+++ b/drivers/net/phy/phy-c45.c
@@ -611,6 +611,7 @@ int genphy_c45_read_pma(struct phy_device *phydev)
 		phydev->speed = SPEED_10;
 		break;
 	case MDIO_PMA_CTRL1_SPEED100:
+		pr_info("yyj debug: %s:%d", __func__, __LINE__);
 		phydev->speed = SPEED_100;
 		break;
 	case MDIO_PMA_CTRL1_SPEED1000:
diff --git a/drivers/net/phy/phy.c b/drivers/net/phy/phy.c
index 4f3e742907cb..c61aeca23da3 100644
--- a/drivers/net/phy/phy.c
+++ b/drivers/net/phy/phy.c
@@ -378,9 +378,12 @@ int phy_mii_ioctl(struct phy_device *phydev, struct ifreq *ifr, int cmd)
 						phydev->duplex = DUPLEX_HALF;
 					if (val & BMCR_SPEED1000)
 						phydev->speed = SPEED_1000;
-					else if (val & BMCR_SPEED100)
+					else if (val & BMCR_SPEED100) {
+						pr_info("yyj debug: %s:%d",
+							__func__, __LINE__);
 						phydev->speed = SPEED_100;
-					else phydev->speed = SPEED_10;
+					} else
+						phydev->speed = SPEED_10;
 				} else {
 					if (phydev->autoneg == AUTONEG_DISABLE)
 						change_autoneg = true;
@@ -522,6 +525,7 @@ int __phy_hwtstamp_set(struct phy_device *phydev,
  */
 void phy_queue_state_machine(struct phy_device *phydev, unsigned long jiffies)
 {
+	dump_stack();
 	mod_delayed_work(system_power_efficient_wq, &phydev->state_queue,
 			 jiffies);
 }
@@ -1404,6 +1408,9 @@ static enum phy_state_work _phy_state_machine(struct phy_device *phydev)
 	bool finished = false;
 	int err = 0;
 
+	pr_info("yyj debug %s:%d phydev->state=%d", __func__, __LINE__,
+		phydev->state);
+
 	switch (phydev->state) {
 	case PHY_DOWN:
 	case PHY_READY:
diff --git a/drivers/net/phy/phy_device.c b/drivers/net/phy/phy_device.c
index b26bb33cd1d4..6ac47ec6d327 100644
--- a/drivers/net/phy/phy_device.c
+++ b/drivers/net/phy/phy_device.c
@@ -2623,9 +2623,10 @@ int genphy_read_status_fixed(struct phy_device *phydev)
 
 	if (bmcr & BMCR_SPEED1000)
 		phydev->speed = SPEED_1000;
-	else if (bmcr & BMCR_SPEED100)
+	else if (bmcr & BMCR_SPEED100) {
 		phydev->speed = SPEED_100;
-	else
+		pr_info("yyj debug: %s:%d", __func__, __LINE__);
+	} else
 		phydev->speed = SPEED_10;
 
 	return 0;
diff --git a/drivers/net/phy/phylink.c b/drivers/net/phy/phylink.c
index 30a654e98352..beeb3e546a37 100644
--- a/drivers/net/phy/phylink.c
+++ b/drivers/net/phy/phylink.c
@@ -3649,6 +3649,8 @@ void phylink_mii_c22_pcs_decode_state(struct phylink_link_state *state,
 					       state->advertising))
 		return;
 
+	pr_info("yyj debug: %s:%d state->interface=%d", __func__, __LINE__,
+		state->interface);
 	switch (state->interface) {
 	case PHY_INTERFACE_MODE_1000BASEX:
 		phylink_decode_c37_word(state, lpa, SPEED_1000);
diff --git a/drivers/net/phy/qcom/qcom-phy-lib.c b/drivers/net/phy/qcom/qcom-phy-lib.c
index d28815ef56bb..a7e5f3137e1a 100644
--- a/drivers/net/phy/qcom/qcom-phy-lib.c
+++ b/drivers/net/phy/qcom/qcom-phy-lib.c
@@ -230,6 +230,7 @@ int at803x_read_specific_status(struct phy_device *phydev,
 			phydev->speed = SPEED_10;
 			break;
 		case AT803X_SS_SPEED_100:
+			pr_info("yyj debug: %s:%d", __func__, __LINE__);
 			phydev->speed = SPEED_100;
 			break;
 		case AT803X_SS_SPEED_1000:
-- 
2.34.1

